rootProject.name = 'wirebarley-test'

include 'adapter'
include 'port'
include 'domain'
include 'application'

for (project in rootProject.children) {
    project.projectDir = file("modules/${project.name}")
}
